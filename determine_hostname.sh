#!/bin/bash

# determine your container hostname here for full automation
# set the DOMAIN env var here system-wide to allow
# for automated SSL generation and deployment

# DOMAIN=${cat /etc/hostname}